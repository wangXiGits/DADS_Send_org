package com.cmcc.mina.state.jiays;

import org.apache.log4j.Logger;
import org.apache.mina.core.session.IoSession;

public interface JiaysPacketHandleState {
	public static Logger log = Logger.getLogger(JiaysPacketHandleState.class);
	public boolean handlePacket(IoSession session,Object message);
}
